	Библиотеки



#define sound_sensor_pin 7 - объявление постоянной через define(не занимает никакого места и в коде) или используется для присваивания переменной кокого нибудь значения
int sound_sensor_pin = 7;  

void setup(){
Serial.begin(9600); - открыть порт (serial - объект для работы с послед портом)

pinMode(pin, mode)  - настроить порт
pin - номер порта(аналоговые(14 - 19 или А0-А5) или цифровые(0 - 13));
mode(INPUT/OUTPUT(выдает 0 или 5 В)/INPUT_PULLUP) - режим работы

}
void loop(){

	Ввод и вывод инфы с монитора порта 
printl("asdf") - выводит текст в консоль с enteroм после
Serial.available() - узает, есть ли данные на ардуинке
Serial.read() - читает инфу с консоли(при этом переводет в данные таблицы ASSKI)
int a=Serial.read()-'0' - уже введе именно цифру, но только последнюю
int a=Serial parseInt() - принимает все число

serial.flush - очисчает буффер порта

	функции времени
delay(t) - останавливается на t миллисекунд
delayMiroseconds(t) - останавливается на t микросекунд


millis() - возвращает колво миллисекунд, прошедших с момента включения МК
if(millis()-last_time>5000)                 - включает данный блок раз в 5 сек (лучше delay, поскольку не тормозит весь код и можно использовать на огромный промежуток времени(23 дня))
	{
		last_time=millis();
	}
или
if(round(millis()/1000)%2==0){}


	Работа с цифровыми портами
pinMode(pin, mode)  - настроить порт
pin - номер порта(аналоговые(14 - 19 или А0-А5) или цифровые(0 - 13));
mode(INPUT/OUTPUT(выдает 0 или 5 В)/INPUT_PULLUP(подключается к земле)) - режим работы

digitalWrite(pin, signal) - выводит напряжение
signal (LOW, или 0, или false - 0 вольт/HIGH, или 1, или true - 5 вольт)

digitaRead(pin) - считывает напряжение
!digitaRead(pin) - инвентировать сигнал
}